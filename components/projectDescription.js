const ProjectDescription = ({ projDesc }) => {
  return (
    <div>
      <h2 className="egg mt-5">project description</h2>
      <p className="green">{projDesc}</p>
    </div>
  );
};

export default ProjectDescription;
